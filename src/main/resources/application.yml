quarkus:
  log:
    console:
      enable: true
      json: false
      level: DEBUG
  native:
    container-build: false

model:
  path: ${MODEL_PATH:model/yolov5x.onnx.zip}
  name: ${MODEL_NAME:yolov5x.onnx}

env:
  debug: false

video:
  path: examples/example.avi